# MySafeFob — managed dependencies (ESP-IDF component manager).
# LVGL is back (ADR-010 amended, docs/ROADMAP.md) as a per-component
# dependency of boards/x4pro/app only (its own idf_component.yml) —
# the "main" component itself still needs nothing from the registry.
dependencies: {}
